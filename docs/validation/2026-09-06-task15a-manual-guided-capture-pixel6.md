# Task 15A manual guided-capture validation — Pixel 6 — 6 September 2026

Result: **4/4 exact authorized Android methods passed**, with zero failures or skips, in 5.235 seconds on the corrected final APK pair. Host verification on the same final source passed 690/690 unit tests, lint with zero errors and 14 warnings, and debug/release/Android-test assembly.

The user separately authorized data-preserving installation of the corrected frozen pair, camera permission, the four methods below, three transient rear-camera photos, generated test cleanup, and removal of the instrumentation package. No additional test class, photo picker, existing personal-media read, MediaStore operation, audio test, app-data clear, or release deployment ran.

## Device and final artifacts

- Device: Pixel 6 (`oriole`), Android 16.
- Build fingerprint: `google/oriole/oriole:16/CP1A.260305.018/14887507:user/release-keys`.
- Source: local `codex/development-takeover` work based on `32a1429`; no production or test source changed between building the corrected final APK pair and completing the successful run.

| Artifact | Final local and installed SHA-256 |
|---|---|
| `app/build/outputs/apk/debug/app-debug.apk` | `336e4f662f98beee6a52a370fec658b97dbdf6ba47fe9163b102d584ccbacad7` |
| `app/build/outputs/apk/androidTest/debug/app-debug-androidTest.apk` | `65637d4a7958c7d6b39cb266f90d0c46980547a8e1fec52b227b79e63e9e8a96` |
| `app/build/outputs/apk/release/app-release-unsigned.apk` — host build evidence only | `12064e8412632cb454c973f822cd8f243524d3494eb0bb235d940ca0faede94c` |

Both tested packages installed successfully using data-preserving replacement. Hashes read from the installed `base.apk` files exactly matched the final local app and instrumentation APKs.

## Exact method results

| Class | Method | Result |
|---|---|---|
| `ui.camera.GuidedCameraScreenTest` | `readyControlsExposeCurrentPoseAndInvokeManualCaptureAndStop` | PASS |
| `ui.camera.GuidedCameraScreenTest` | `captureRequiresReadyCameraAndStoppingDisablesBothActions` | PASS |
| `camera.JournaledGuidedCaptureIntegrationAndroidTest` | `generatedManualCapturePublishesExactlyThreeThenConfirmsAndAdvancesOnce` | PASS |
| `camera.JournaledGuidedCaptureIntegrationAndroidTest` | `authorizedPixelRealCameraPublishesExactlyThreeThenConfirmsAndAdvancesOnce` | PASS |

All class names use the `com.tonyisup.poseguidesnap` prefix. The runner was `com.tonyisup.poseguidesnap.test/androidx.test.runner.AndroidJUnitRunner`. One exact comma-separated `class` selector named only these four methods.

The [raw final instrumentation output](2026-09-06-task15a-manual-guided-capture-pixel6-output.txt) records every successful `(class, method)` pair followed by `OK (4 tests)`. Output SHA-256: `d090716474ecb3738cc96d3b21a7d2a25fee410a5587ca71987bf266f7403ae9`.

## Device-found correction

The first live candidate used app SHA-256 `3c9a5109f692654479ea91bbdad3d126867980e1e6f8a679193cc5d776b843ce`, instrumentation SHA-256 `75e72c7025a68cd584d499cee0ebbadb9a32aedeb7cc24c3e09ca261d8db0ec5`, and unsigned-release SHA-256 `e91db9a5297c35e9ac81a171dea3a1da647fe817a18cef065c8a028b511a620d`.

Its first invocation ran while the secure device remained locked. The generated-data integration passed, while both Compose methods had no launched test host and the live method could not bind the camera. After the Pixel was physically unlocked, the unchanged pair passed both Compose methods and generated-data integration. The live method bound the rear camera and CameraX reported a successful first save, but the coordinator correctly withheld durability.

CameraX 1.6.2's file-target path saves through an internal temporary file, deletes the caller's destination, and renames its temporary file into place. That behavior replaced the journal-owned temporary inode created after Room admission. The private store's identity check rejected the replacement, so the attempt did not confirm or advance and test teardown removed its generated authority and files. The [raw unlocked first-candidate output](2026-09-06-task15a-manual-guided-capture-pixel6-candidate1-output.txt) records the 3/4 result; its SHA-256 is `d6b3639fb6077b278a84e66b11aeaf83d36ceded4a3938103dd2a6c634e026dd`.

The correction opens the already-admitted temporary file and gives CameraX its output stream. CameraX copies the JPEG bytes into that open file; the adapter closes the stream before reporting success, and the store then verifies the same inode before sync, hashing, publication, and final durability. The complete host gate passed again, new hashes were frozen, and the corrected pair received separate authorization before the final 4/4 run.

## What this proves

On the corrected APK pair and Pixel 6:

- The manual controls expose the current pose and reference, invoke Capture and Stop, require camera readiness, and disable both actions while stopping.
- Generated bytes traversed the reducer, real Room V4 registration and per-file journal, exact app-private publication, journal-derived confirmation, immutable outputs/outbox, and one advance.
- The real rear CameraX path traversed that same coordinator. It produced exactly three positive, decodable app-private images, left each journal row `FINAL_DURABLE` before confirmation, consumed exactly three journal rows, recorded exactly three immutable private outputs and export outputs, recorded one receipt/outbox, and advanced once.
- The journal's stable-file check causally rejected CameraX's original inode-replacing adapter, and the corrected stream adapter preserved that identity on hardware.

This bounded checkpoint does not prove the complete production started-session UI path with a user-created shoot, automatic matching or shutter behavior, calibration thresholds, process interruption during a live capture, MediaStore export, product deletion, audio, TalkBack usability, image quality, or the full Gate 2 matrix.

## Cleanup

Each integration method used a generated UUID-named Room database and generated UUID-named root below `noBackupFilesDir`. Teardown removed those databases, SQLite/Room sidecars, generated files, and directories. Post-run exact-pattern checks returned no residue. Camera service reported no active client. The instrumentation package was uninstalled and confirmed absent, temporary host copies of installed APKs were removed, and the corrected main app remains installed with its data preserved.

This bounded run completes the Task 15A device checkpoint. Gate 2 remains open for the complete selected-reference production UI and acceptance matrix.
