# Task 16AC — knee-relative hand checks

Task 16AB completed one authorized camera run, timed out before hold/measurement, and was cleaned up. RIGHT_HAND_ON_KNEE completed three times with no Good. Terminal stillness passed; ARM_POSITION, FRAMING and UNCONFIRMED_ADJUSTMENT remained. The participant reported the hand already supported at the first instruction and no clear adjustment from the repetitions.

Fixed the instruction/acceptance mismatch: wrists now target same-side observed knees; elbows retain reference targets and select the normal/mirrored reference independently of wrist error. Qualified knees required; radius remains 0.25. Projected proximity cannot verify contact, palm orientation or the full pose. Added one fixed inability-to-confirm explanation per unresolved hand request, suppressing further identical hand commands while keeping confirmation and readiness pending. Good/new-session reset the relevant notice budget. Motion/timing and production thresholds unchanged.

Five new regressions failed before the fix and passed after it; a sixth guards mirrored elbow selection. Full gate: 774/774 host tests, main/test assembly, lint 0 errors/9 warnings. See the [validation record](../../docs/validation/2026-09-25-task16ac-knee-relative-coaching.md) for exact hashes, limitations and the next protocol. Main installed/hash verified with app data preserved; test/export absent, no own camera client. At the end of implementation, no AC camera/audio run had occurred; the later result is below. Dataset pixel6-knee-relative-a, sequence positive-knee-relative-landscape-a and task16ac-camera-output.txt were unused at implementation and are now consumed. Do not reuse AB identifiers or start a camera run merely because installation is complete.

The AC physical run has now consumed those identifiers; see the [Pixel record](../../docs/validation/2026-09-25-task16ac-knee-relative-pixel6.md). One right-hand instruction plus one explanation, no Good, readiness timeout; participant confirmed support throughout and clear explanation. Cleanup passed. Task 16AD is the next unused diagnostic protocol; do not repeat AC.
